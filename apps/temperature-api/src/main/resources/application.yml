spring:
  application:
    name: temperature-api

server:
  port: 8081

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    yandex.practicum: DEBUG